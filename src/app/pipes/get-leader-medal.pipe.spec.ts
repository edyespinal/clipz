import { GetLeaderMedalPipe } from './get-leader-medal.pipe';

describe('GetLeaderMedalPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLeaderMedalPipe();
    expect(pipe).toBeTruthy();
  });
});
