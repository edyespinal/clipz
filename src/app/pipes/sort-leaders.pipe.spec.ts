import { SortLeadersPipe } from './sort-leaders.pipe';

describe('SortLeadersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortLeadersPipe();
    expect(pipe).toBeTruthy();
  });
});
